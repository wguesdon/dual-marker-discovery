# Claude tooling log

Evidence of how Claude Code and Claude Science were used, for the "Claude Use" judging criterion (25%).
One short entry per meaningful handoff: what was asked, what came back, and what changed as a result.
Log disagreements too, a tool that was overruled is more informative than one that was rubber-stamped.

| Date | Tool | Task | Outcome / what changed |
|---|---|---|---|
| 2026-07-13 | Claude Code | Indication scoping: compared prostate, ovarian, colorectal for AND/NOT-gate discovery against the constraints (re-discoverable benchmark, single-cell depth, protein coverage). Verified current clinical programs (PSMA-PSCA, STEAP1 engager, A2 Bio Tmod) via web search. | Chose prostate: only solid tumor with a re-discoverable AND-gate positive control (PSMA-PSCA). |
| 2026-07-13 | Claude Code | Scaffolded the repository skeleton (mirrors the SatMut project layout). | Repo initialized: numbered-script pipeline, tables-bound Quarto report, tests, Claude Science task folder. |
| 2026-07-13 | Claude Code (3 parallel Explore agents) | Dispatched three read-only research agents at once to verify tumor and healthy data access and locate the March-2025 logic-gated prior art. | Chose the 24-patient CELLxGENE localised-PCa `.h5ad` over HuPSA (ready-annotated h5ad vs a 3.8 GB Seurat RDS needing conversion); locked the Census subset recipe; differentiated from LogiCAR (breast, pooled cells) by per-patient scoring plus PSMA-PSCA recovery. |
| 2026-07-13 | Claude Code | Debugged a dependency-resolver trap: `numpy>=2.2` forced an ancient `numba` 0.53.1 that supports neither Python 3.11 nor 3.13. | Pinned `numpy<2` on Python 3.11, which resolved `tiledbsoma` 2.3 / `cellxgene-census` 1.18 from wheels. The research agent's "py3.13 is fine" claim was wrong; Claude Code caught and fixed it. |
| 2026-07-13 | Claude Code | Rule-1 access gate: inspected the tumor `.obs` rather than assuming a malignant definition. | Surfaced the authors' copy-number/signature `malignant_anno_merged` column (3,802 malignant cells), replacing a weaker "luminal cells are malignant" proxy. |
| 2026-07-13 | **Human corrected Claude Code** | The human flagged that a purely extra-prostatic safety metric would pass a marker expressed across all prostate cells, which would destroy the whole gland and is not tumor-specific. | Added a **matched benign-prostate control** (adjacent-benign/normal epithelium from the same patients); nominations must separate malignant from benign prostate, not just from other organs. |
| 2026-07-13 | **Claude Code overruled a data source** | The Human Protein Atlas immunofluorescence localization calls PSMA, STEAP1, STEAP2 and CD276 "not plasma membrane" — false negatives for validated surface targets. | Did **not** use HPA localization as a hard surface gate; used curated membrane topology instead, but kept HPA to demote intracellular prostein (SLC45A3, vesicular) from nomination. |
| 2026-07-13 | Claude Code + Podman + R (scDblFinder) | Doublet detection via a write-to-disk Python->R->Python handoff in a Bioconductor container (sc-best-practices method), so AND co-expression is not inflated by two-cells-as-one. | 4.8% of cells (5.6% of malignant) called doublet; removing them shifts nominated-pair AND coverage by <1.1%, so the finding is not a doublet artifact. |
| 2026-07-13 | Claude Code + Nextflow | Evaluated nf-core/scdownstream for the downstream single-cell pass: installed a user-level JDK + Nextflow, configured the podman profile, pulled the pipeline. | Found it pre-release (no stable tag; `master` still template-scaffolded, functional code on `dev`). Chose the focused scDblFinder container for a reproducible same-day result and documented scdownstream (`dev`) as the future full-pipeline path. |
| 2026-07-13 | Two parallel Claude Code agents (shared tree) | Split the build: one agent drove data, scoring, positive/negative controls, nomination and doublet QC; a second drove the Quarto report, threshold sensitivity and README. | Both committed to one working tree with path-scoped commits, roughly halving wall-clock to a complete submission. |
