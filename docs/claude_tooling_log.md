# Claude tooling log

Evidence of how Claude Code and Claude Science were used, for the "Claude Use" judging criterion (25%).
One short entry per meaningful handoff: what was asked, what came back, and what changed as a result.
Log disagreements too, a tool that was overruled is more informative than one that was rubber-stamped.

| Date | Tool | Task | Outcome / what changed |
|---|---|---|---|
| 2026-07-13 | Claude Code | Indication scoping: compared prostate, ovarian, colorectal for AND/NOT-gate discovery against the constraints (re-discoverable benchmark, single-cell depth, protein coverage). Verified current clinical programs (PSMA-PSCA, STEAP1 engager, A2 Bio Tmod) via web search. | Chose prostate: only solid tumor with a re-discoverable AND-gate positive control (PSMA-PSCA). |
| 2026-07-13 | Claude Code | Scaffolded the repository skeleton (mirrors the SatMut project layout). | Repo initialized: numbered-script pipeline, tables-bound Quarto report, tests, Claude Science task folder. |
| 2026-07-13 | Claude Code (3 parallel Explore agents) | Dispatched three read-only research agents at once to verify tumor and healthy data access and locate the March-2025 logic-gated prior art. | Chose the 24-patient CELLxGENE localised-PCa `.h5ad` over HuPSA (ready-annotated h5ad vs a 3.8 GB Seurat RDS needing conversion); locked the Census subset recipe; differentiated from LogiCAR (breast, pooled cells) by per-patient scoring plus PSMA-PSCA recovery. |
| 2026-07-13 | Claude Code | Debugged a dependency-resolver trap: `numpy>=2.2` forced an ancient `numba` 0.53.1 that supports neither Python 3.11 nor 3.13. | Pinned `numpy<2` on Python 3.11, which resolved `tiledbsoma` 2.3 / `cellxgene-census` 1.18 from wheels. The research agent's "py3.13 is fine" claim was wrong; Claude Code caught and fixed it. |
| 2026-07-13 | Claude Code | Rule-1 access gate: inspected the tumor `.obs` rather than assuming a malignant definition. | Surfaced the authors' copy-number/signature `malignant_anno_merged` column (3,802 malignant cells), replacing a weaker "luminal cells are malignant" proxy. |
| 2026-07-13 | **Human corrected Claude Code** | The human flagged that a purely extra-prostatic safety metric would pass a marker expressed across all prostate cells, which would destroy the whole gland and is not tumor-specific. | Added a **matched benign-prostate control** (adjacent-benign/normal epithelium from the same patients); nominations must separate malignant from benign prostate, not just from other organs. |
| 2026-07-13 | **Claude Code overruled a data source** | The Human Protein Atlas immunofluorescence localization calls PSMA, STEAP1, STEAP2 and CD276 "not plasma membrane" — false negatives for validated surface targets. | Did **not** use HPA localization as a hard surface gate; used curated membrane topology instead, but kept HPA to demote intracellular prostein (SLC45A3, vesicular) from nomination. |
| 2026-07-13 | Claude Code + Podman + R (scDblFinder) | Doublet detection via a write-to-disk Python->R->Python handoff in a Bioconductor container (sc-best-practices method), so AND co-expression is not inflated by two-cells-as-one. | 4.8% of cells (5.6% of malignant) called doublet and **removed before scoring** (singlets are the primary input via `load_scan_frames`, script 14). Re-scoring on the 65,040 single cells moves nominated-pair AND coverage by <=1.0 pp (and slightly upward), so the finding is not a doublet artifact. |
| 2026-07-13 | Claude Code + Nextflow | Evaluated nf-core/scdownstream for the downstream single-cell pass: installed a user-level JDK + Nextflow, configured the podman profile, pulled the pipeline. | Found it pre-release (no stable tag; `master` still template-scaffolded, functional code on `dev`). Chose the focused scDblFinder container for a reproducible same-day result and documented scdownstream (`dev`) as the future full-pipeline path. |
| 2026-07-13 | Two parallel Claude Code agents (shared tree) | Split the build: one agent drove data, scoring, positive/negative controls, nomination and doublet QC; a second drove the Quarto report, threshold sensitivity and README. | Both committed to one working tree with path-scoped commits, roughly halving wall-clock to a complete submission. |
| 2026-07-13 | Claude Code (peer-review triage) | A reviewer flagged that mixing scRNA assays under one raw-count threshold could confound the normal-tissue liabilities. Rather than argue, Claude Code checked the raw data: counted assays in the pulled Tabula Sapiens (90% 10x 3' v3, but 3.7% Smart-seq2) and recomputed each headline liability assay-matched and donor-robust. | Found the reported PSMA x STEAP1 worst liability (0.28) was an artifact of 39 single-donor Smart-seq2 cells; assay-matched + donor-robust it is 0.17 (5 donors), close to the PSMA-PSCA benchmark. The positive control held. The reviewer's most urgent concern was real and, once fixed, strengthened the finding. |
| 2026-07-13 | Claude Code subagent (web verification) | Dispatched a background agent to verify two dataset citations against primary sources before they entered the report (Rule 1). | Confirmed the localized-prostate atlas is now published in Cancer Research 2026 (Apostolov et al., DOI 10.1158/0008-5472.CAN-25-1202, PMID 41879555) and that the 1.1M-cell healthy reference is Tabula Sapiens 2.0 (bioRxiv 2024.12.03.626516); citations updated from the preprint and the 2022 Science paper. |
| 2026-07-13 | Claude Code (leakage triage) | A second review alleged HPN/EPCAM leak into the malignant-cell label. Claude Code inspected the cohort obs (found the label is built from `pca_liu_up` / `pca_wallace_up` signatures + CopyKAT), noted a crude CopyKAT split was confounded by purity, and delegated the decisive check to a subagent that read the study's own signature CSVs on GitHub. | Confirmed HPN is in both Liu and Wallace signatures and EPCAM in Liu, while FOLH1/STEAP1/PSCA are in neither. Excluded all HPN/EPCAM pairs from nomination (flagged, held off the frontier); with the leaked pairs removed the clean PSMA x STEAP1 lead became Pareto-non-dominated. |
| 2026-07-13 | Claude Code subagent (web verification) | Verified ABBV-969, which the reviewer said makes the PSMA x STEAP1 pair non-novel. | Confirmed AbbVie's dual PSMA/STEAP1 ADC, phase 1 NCT06318273 (ASCO 2026, 49 patients, ORR 45%); it binds either antigen (OR-like), so it does not validate a strict AND gate. Cited it and delimited novelty: the contribution is the AND framing, not the pair. |
| 2026-07-13 | Claude Code + Podman + R (Seurat V5) | Added a second cohort (HuPSA, 3.6 GB Seurat V5 .rds). No local R, so pulled `docker.io/satijalab/seurat:5.0.0` and used the write-to-disk handoff to subset the 369k-cell object to the 29 panel genes and export counts + metadata for Python to score. | Cross-cohort replication: tumor-specificity held (malignant vs benign +0.15, 83% of samples) but PSMA x STEAP1 coverage collapsed in advanced disease (AR+ adenocarcinoma ~0.35 -> ~0 in NEPC/double-negative, 0.05 in mCRPC). The independent cohort turned an asserted design goal into a measured, disconfirming result on generalization, which materially reframed the lead. |
| 2026-07-13 | Claude Science (CS08) | Normal-tissue liabilities of the 10 candidate surface antigens, pulled from GTEx v8 medians and the Human Protein Atlas (RNA + IHC with reliability categories) rather than inferred from the scRNA healthy reference. | Produced `results/figures/fig_s1_gtex_normal_tissue_tpm.png`, a bulk-tissue safety map independent of our scRNA co-detection estimate: FOLH1/PSMA high in brain and salivary gland, PSCA very high in stomach and bladder, HPN in liver and kidney, TACSTD2/TROP2 broad across skin and esophagus, DLL3 low everywhere (CNS-restricted). Committed as a supplementary figure; unlike `fig1`-`fig5` it is **not** produced by `scripts/90_demo_figures.py`, it comes from the workbench, so it corroborates the scRNA liabilities from an orthogonal source rather than re-deriving them. |
